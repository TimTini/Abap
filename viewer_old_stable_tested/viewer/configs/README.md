# Legacy

This folder is no longer used by the viewer.

- Viewer configs are now generated from `/configs/*.json` into `viewer/configs.generated/*.js`
- Regenerate with: `node scripts/build-viewer-configs.js`
